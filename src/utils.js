const isAlpha = (str) => /^[a-zA-Z]*$/.test(str);
const isNumeric = (str) => /^\d*$/.test(str);
const API_KEY = '095d5387-cca8-40cd-bdac-e568d5437dfa';

export { isAlpha, isNumeric, API_KEY };

export default { isAlpha, isNumeric, API_KEY };
